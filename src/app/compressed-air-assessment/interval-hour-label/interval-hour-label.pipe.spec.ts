import { IntervalHourLabelPipe } from './interval-hour-label.pipe';

describe('IntervalHourLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new IntervalHourLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
