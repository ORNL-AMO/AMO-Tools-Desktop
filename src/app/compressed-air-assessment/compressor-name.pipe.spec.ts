import { CompressorNamePipe } from './compressor-name.pipe';

describe('CompressorNamePipe', () => {
  it('create an instance', () => {
    const pipe = new CompressorNamePipe();
    expect(pipe).toBeTruthy();
  });
});
