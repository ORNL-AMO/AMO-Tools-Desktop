import { DayTypeFilterPipe } from './day-type-filter.pipe';

describe('DayTypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DayTypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
