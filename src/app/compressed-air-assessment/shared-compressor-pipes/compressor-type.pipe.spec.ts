import { CompressorTypePipe } from './compressor-type.pipe';

describe('CompressorTypePipe', () => {
  it('create an instance', () => {
    const pipe = new CompressorTypePipe();
    expect(pipe).toBeTruthy();
  });
});
