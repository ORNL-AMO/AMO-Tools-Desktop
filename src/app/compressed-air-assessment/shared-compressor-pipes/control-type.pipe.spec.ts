import { ControlTypePipe } from './control-type.pipe';

describe('ControlTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ControlTypePipe();
    expect(pipe).toBeTruthy();
  });
});
