import { FilterCompressorsPipe } from './filter-compressors.pipe';

describe('FilterCompressorsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCompressorsPipe();
    expect(pipe).toBeTruthy();
  });
});
