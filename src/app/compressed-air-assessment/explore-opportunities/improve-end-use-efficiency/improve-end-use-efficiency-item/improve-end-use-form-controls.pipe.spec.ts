import { ImproveEndUseFormControlsPipe } from './improve-end-use-form-controls.pipe';

describe('ImproveEndUseFormControlsPipe', () => {
  it('create an instance', () => {
    const pipe = new ImproveEndUseFormControlsPipe();
    expect(pipe).toBeTruthy();
  });
});
