import { ChillerCompressorTypePipe } from './chiller-compressor-type.pipe';

describe('ChillerCompressorTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ChillerCompressorTypePipe();
    expect(pipe).toBeTruthy();
  });
});
