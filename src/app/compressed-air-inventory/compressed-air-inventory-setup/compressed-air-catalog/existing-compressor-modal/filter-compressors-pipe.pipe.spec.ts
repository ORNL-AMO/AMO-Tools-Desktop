import { FilterCompressorsPipePipe } from './filter-compressors-pipe.pipe';

describe('FilterCompressorsPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCompressorsPipePipe();
    expect(pipe).toBeTruthy();
  });
});
