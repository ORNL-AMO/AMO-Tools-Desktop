import { CompressedAirInventorySummaryTablePipe } from './compressed-air-inventory-summary-table.pipe';

describe('CompressedAirInventorySummaryTablePipe', () => {
  it('create an instance', () => {
    const pipe = new CompressedAirInventorySummaryTablePipe();
    expect(pipe).toBeTruthy();
  });
});
