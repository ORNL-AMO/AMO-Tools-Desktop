import { PumpSummaryTablePipe } from './pump-summary-table.pipe';

describe('PumpSummaryTablePipe', () => {
  it('create an instance', () => {
    const pipe = new PumpSummaryTablePipe();
    expect(pipe).toBeTruthy();
  });
});
