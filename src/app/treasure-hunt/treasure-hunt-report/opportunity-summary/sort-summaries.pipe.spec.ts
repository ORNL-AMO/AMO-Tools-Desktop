import { SortSummariesPipe } from './sort-summaries.pipe';

describe('SortSummariesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortSummariesPipe();
    expect(pipe).toBeTruthy();
  });
});
