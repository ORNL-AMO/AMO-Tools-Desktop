import { SortCardsByPipe } from './sort-cards-by.pipe';

describe('SortCardsByPipe', () => {
  it('create an instance', () => {
    const pipe = new SortCardsByPipe();
    expect(pipe).toBeTruthy();
  });
});
