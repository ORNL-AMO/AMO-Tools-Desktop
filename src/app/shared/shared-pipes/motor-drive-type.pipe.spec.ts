import { MotorDriveTypePipe } from './motor-drive-type.pipe';

describe('MotorDriveTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MotorDriveTypePipe();
    expect(pipe).toBeTruthy();
  });
});
