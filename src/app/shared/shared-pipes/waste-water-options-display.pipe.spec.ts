import { WasteWaterOptionsDisplayPipe } from './waste-water-options-display.pipe';

describe('WasteWaterOptionsDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new WasteWaterOptionsDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
