import { OptionDisplayValuePipe } from './option-display-value.pipe';

describe('OptionDisplayValuePipe', () => {
  it('create an instance', () => {
    const pipe = new OptionDisplayValuePipe();
    expect(pipe).toBeTruthy();
  });
});
