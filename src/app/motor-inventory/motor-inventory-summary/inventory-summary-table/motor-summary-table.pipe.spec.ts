import { MotorSummaryTablePipe } from './motor-summary-table.pipe';

describe('MotorSummaryTablePipe', () => {
  it('create an instance', () => {
    const pipe = new MotorSummaryTablePipe();
    expect(pipe).toBeTruthy();
  });
});
