import { FilterMotorOptionsPipe } from './filter-motor-options.pipe';

describe('FilterMotorOptionsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMotorOptionsPipe();
    expect(pipe).toBeTruthy();
  });
});
