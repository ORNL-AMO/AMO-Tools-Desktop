import { BatchAnalysisTablePipe } from './batch-analysis-table.pipe';

describe('BatchAnalysisTablePipe', () => {
  it('create an instance', () => {
    const pipe = new BatchAnalysisTablePipe();
    expect(pipe).toBeTruthy();
  });
});
